def pptx2markdown(pptx_file_path: str) -> str:
    """
    读取 .pptx 文件，将其内容提取并转换为 Markdown 格式的字符串。

    参数:
        pptx_file_path: .pptx 文件路径（相对路径或绝对路径均可）

    返回:
        Markdown 格式的文本，包含各页幻灯片的标题和正文内容
    """
    pass
